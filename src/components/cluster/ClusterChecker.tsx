// src/components/cluster/ClusterChecker.tsx
import React, { ReactNode } from 'react'

export function ClusterChecker({ children }: { children?: ReactNode }) {
  // ton code ici...
  return <>{children}</>
}
